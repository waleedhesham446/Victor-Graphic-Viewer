<Window x:Class="Victor_Graphic_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Victor_Graphic_Viewer"
        mc:Ignorable="d"
        Title="WSCAD Vector Viewer" Height="450" Width="800">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Open JSON/XML…" Click="OpenFile_Click"/>
        </ToolBar>

        <Border DockPanel.Dock="Right" Width="200"
                BorderBrush="#CCCCCC" BorderThickness="1,0,0,0"
                Background="#F9F9F9">
            <StackPanel Margin="8">
                <TextBlock Text="Inspector" FontWeight="Bold" Margin="0,0,0,8"/>
                <ItemsControl x:Name="PropertyList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Key}"
                                           Foreground="#888" FontSize="11"/>
                                <TextBlock Grid.Column="1" Text="{Binding Value}"
                                           FontSize="11" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
        
        <local:ShapeCanvas x:Name="shapeCanvas" />
    </DockPanel>
</Window>
